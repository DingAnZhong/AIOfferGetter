# Sync Helper 用户手册

> 本手册只讲怎么用。风险提示：本工具用于辅助笔试与面试，违反考试/面试规则的风险由使用者自担。

**注意：**

- 因本软件微信截图无法捕获，腾讯会议屏幕共享无法识别，故不提供截图教程。
- 并且 macOS 的支持不足，可能很容易就无法使用，仅提供 Windows 版本。

## 1. 安装与启动

1. 运行安装包（`Sync Helper Setup x.x.x.exe`），像普通软件一样下一步装完即可。
2. 从开始菜单或桌面启动 **Sync Helper**。
3. 第一次启动会弹出**激活窗口**（见下一节）；激活成功后直接进入主界面，不需要重启。
4. 之后每次启动不再弹激活窗（除非密钥过期）。

## 2. 激活（首次使用必做）

软件需要密钥才能使用，一把密钥只属于一台电脑、有到期日。

1. 首次启动的激活窗口里有一行 16 位**机器码**，点旁边的**复制**按钮。
2. 把机器码发给你获取密钥的提供者，说明你想要的**使用时长**。
3. 拿到一串 `AIOH1.` 开头的密钥后，粘贴进激活窗口的输入框——**粘贴后自动激活**，窗口关闭、直接进入主界面。
4. 如果提示错误：
   - **密钥无效**：没复制完整，重新复制整串再粘贴；
   - **密钥已过期**：找提供者换新密钥；
   - **密钥不属于本机**：这把密钥是发给别的电脑的。

注意：

- 重装 Windows 后机器码会变，需要重新要一把密钥。
- 把整台电脑的配置文件拷到别的电脑上用不了，密钥认机器不认文件。
- 密钥到期后，下次启动会重新弹激活窗口，换一把新密钥即可，原有设置不丢。

## 3. 首次配置（激活后）

点主界面右上角 **⚙** 打开设置，必填三项：

- **Base URL**：AI 服务地址（OpenAI 兼容格式，问提供者或服务商会给）；
- **API Key**：AI 服务的密钥（和激活密钥是两回事）；
- **Model**：模型名。

推荐填：

- **ASR 模型**：语音模型，可以转写面试官的问题。思考开关建议关闭，这样回复的快一点。

点**保存**立即生效，重装升级都不会丢。其它设置项的作用见第 7 节。

推荐使用**小米**的服务：`https://platform.xiaomimimo.com/console/plan-manage`

因为一个服务就有 **ASR** 语音识别和 **CHAT** 代码生成的能力，**OCR** 图片文字提取是嵌入功能。

多模态文字提取都不保证 **100%** 准确。

## 4. 日常操作速查

| 我要做什么 | 怎么做 |
| --- | --- |
| 截屏一道题目，让 AI 答 | 按 `Ctrl+Shift+Alt+Z`，拖框选中题目，点**确定**；识别出的文字可先改正再发送 |
| 手动打字提问 | 按 `Ctrl+Shift+Alt+X`，输入框停靠在窗口底部（对话记录仍在上方可见），输入后 Enter 发送（Shift+Enter 换行，Esc 关闭） |
| 看答案 | 答案在主窗口流式显示，滚轮可上下滚动 |
| 答案太慢/太快 | `Ctrl+Shift+Alt+↑ / ↓` 调自动滚动速度 |
| 暂停自动滚动 | `Ctrl+Shift+Alt+C` |
| 手动滚动答案 | 鼠标滚轮，或 `Ctrl+Shift+Alt+← / →` |
| 把答案打进答题框 | 答案生成后按 `Ctrl+Shift+Alt+B`，3 秒倒计时内点进答题输入框，它会模拟人工逐字打字；打字过程中按 `Esc` 中断 |
| 听到面试官提问（仅 Windows） | 按 `Ctrl+Shift+Alt+V` 开始/停止收音，系统声音会被转成文字并直接让 AI 作答 |
| 紧急隐藏一切 | `Ctrl+Shift+Alt+H`，再按一次恢复 |
| 开启新会话 | `Ctrl+Shift+Alt+S`：清空对话上下文。同一场笔试/面试里的所有提问共享上下文（可以追问"刚才那题换个思路"），换下一场时按一下这个键即可 |

所有热键都可以在设置页改成别的组合（Esc 中断键入这一项固定）。如果保存热键后报"热键注册失败"，说明那个组合被别的软件占了，换一个。

**如何退出**：窗口上方右键点击，会出现最小化和退出等选项。

## 5. 隐身说明（重要）

- 这个窗口**不会出现在屏幕共享、录屏、别人的截图里**，也不进任务栏和 Alt+Tab，窗口标题显示为无害的 "Settings"。
- 启动几秒后程序会**自检**：如果状态栏出现红色"⚠ 自检失败"，说明当前环境防隐身可能失效（老系统、特殊采集软件等），**当天不要依赖它**；若提示"自检未能判定"，自己用 `Win+Shift+S` 截一张全屏确认窗口在不在图里。
  - **注意**：仅为可能失效，可以先开微信截图或腾讯会议试一下效果。
- **透明度调低 ≠ 隐身**，两个是独立能力：防采集靠系统保护，调透明度是为了让旁边的人看不清。两者可以同时用。
- 透明度可以低到 0.1，但低于 0.3 自己也可能看不清。更推荐的做法：保持中等透明度，把**文字色调暗**（比如 `#55555f`）、字号调小，一米外就看不清内容。
- **变色龙模式**：背景自动融入身后画面，视觉上接近消失。开启后每 2 秒截一次屏取色，纯色桌面/IDE 下效果最好，背景花哨时会变成"混色"。
- 万一被同事/家人远程看到窗口，按 `Ctrl+Shift+Alt+H` 立刻消失。

## 6. 使用建议

- **答案约 5 秒开始出字是正常的**（模型思考 + 网络延迟）。嫌慢可以在设置里把思考模式选"关闭思考"、思考强度选 low，速度明显变快，但难题的回答质量会略降。
- **对话是有记忆的**：同一场考试里的提问会带着之前的问答一起发给 AI，方便追问；超过约 1.6 万字后自动丢最旧的问答。换下一场考试记得按新会话热键，否则上一场的题目还在上下文里。
- auto-typer 默认关闭。开启后它也只是待命，必须你按热键才开始打字——这是防止误触发。
- 语音通道只收**系统播放的声音**（面试软件里对方的声音），不会收麦克风，你自己的说话声不会被录。
- 设置改完忘记保存就关窗口 = 没改。改完点保存。

## 7. 设置项说明

| 设置 | 说明 |
| --- | --- |
| Base URL / API Key / Model | AI 服务三件套，见第 3 节 |
| 透明度 | 0.1（几乎看不见）到 1（不透明），见第 5 节 |
| 透明窗口 | 默认开。若自检报警，先关掉它排查 |
| 背景色 / 文字色 / 字号 | 外观调节；文字色调暗 = 防偷瞄 |
| 截图框色 | 框选题目时矩形边框的颜色，默认蓝色；屏幕背景与框色接近看不清时换个颜色。框选全程屏幕不变暗、鼠标样式不变 |
| 截图框/按钮不透明度 | 框选时选框、确定/取消按钮、提示条的淡入程度，数值越小越不显眼 |
| 变色龙 | 背景自动融入身后画面（含顶部 Settings 条），开启后立即生效、每 2 秒刷新；采样是取画面平均色，桌面深色区域多时颜色会偏深。开启期间手动"背景色"无效（输入框会禁用） |
| 思考强度 | 默认 / low / medium / high。用普通非推理模型（gpt-4o 类）请保持"默认"，选其它档可能报错 |
| 思考模式 | 用 DeepSeek 等默认开思考的模型觉得慢，选"关闭思考"；其它服务商保持"跟随模型默认" |
| 代码题语言 | 填 go、java 等，代码题按该语言作答，留空自动判断 |
| 个人知识库目录 | 填一个本地文件夹路径，里面的简历、项目介绍等资料（支持 txt/md/docx，含子文件夹）会在每次提问时自动提供给 AI——自我介绍、项目经历类问题会用你的真实背景回答，不再编造。留空关闭。改文件夹里的文件后下次提问即生效 |
| auto-typer | 见第 4、6 节，默认关 |
| 打字速度范围 | auto-typer 每次按键的间隔区间（毫秒），区间越大越像人 |
| 语音模式 | 手动 = 按热键开始/停止；持续监听 = 开着持续听，检测到说话自动转写（仅 Windows） |
| ASR 模型 | 语音转文字的模型名，默认 whisper-1；用小米 MiMo 服务则填 mimo-v2.5-asr |

## 8. 常见问题

| 现象 | 处理 |
| --- | --- |
| 启动弹激活窗口 | 密钥过期/换机器了，按第 2 节换新密钥，设置不会丢 |
| 热键全部没反应 | 大概率有旧实例残留，重启一次软件；还不行就是组合被占用，换组合 |
| 没法往答案区域打字/粘贴 | 这是设计：答题平台会检测粘贴，答案只能看，或用 auto-typer 打 |
| 语音转写报错 | 检查 ASR 模型名与你的服务商是否匹配（第 7 节表格最后一行） |
| 状态栏红色自检失败 | 见第 5 节，当天不要依赖隐身 |
| 软件占用太多内存 | 正常现象，截图识别模型常驻内存；答题完可关掉重开 |
